spring:
  config:
    import: optional:file:.env[.properties]
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://localhost:3306/recipe?characterEncoding=utf8&serverTimezone=UTC
    username: root
    password: tlwkrdmldkdlA!

  jackson:
    serialization:
      write-dates-as-timestamps: false

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true

  batch:
    jdbc:
      initialize-schema: always # Spring Batch 메타데이터 테이블 자동 생성
    job:
      enabled: false # 애플리케이션 시작 시 Job 자동 실행 방지 (수동 실행 위함)

  mail:
    host: smtp.naver.com
    port: 465
    username: stay_on_track@naver.com
    password: KVRG8UGYM9ZJ
    properties:
      mail.smtp.auth: true
      mail.smtp.ssl.enable: true
      #      mail.smtp.starttls.enable: true   # TLS 쓸 땐 587 포트 + 이 옵션 사용
      #      mail.smtp.starttls.required: true

      mail.smtp.timeout: 5000               # 연결 타임아웃
      mail.smtp.connectiontimeout: 5000     # 연결 타임아웃
      mail.smtp.writetimeout: 5000          # 쓰기 타임아웃

  data:
    redis:
      host: localhost
      port: 6379

jwt:
  secret: dI5pBjrtgy9xFHiZtMs3fM7P8OR/wvxrexu/mybWcKc=
  access-token-validity-in-seconds: 3600
  refresh-token-validity-in-seconds: 86400

thymeleaf:
  cache: false
  prefix: classpath:/templates/
  suffix: .html

server:
  port: 8080
  servlet:
    context-path: /studio-recipe

front:
  urls: "http://localhost:5173,https://rapa8.shop,https://www.rapa8.shop"

recommender:
  flask-base-url: http://127.0.0.1:5000

logging.level:
  org.hibernate.SQL: debug
  org.hibernate.orm.jdbc.bind: TRACE
  org.springframework.security.web: TRACE

management:
  endpoints:
    web:
      exposure:
        include: "health"


flask:
  base-url: http://127.0.0.1:5000